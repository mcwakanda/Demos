var APP = {};APP.events = [	{id: 'myEvent1', event: new Event('myEvent1')},	{id: 'myEvent2', event: new Event('myEvent2')},	{id: 'myEvent3', event: new Event('myEvent3')},];APP.getEvent = function(id){	var eventObj;	APP.events.forEach(function(element){		if(element.id == id){			eventObj = element.event;		}	});	return eventObj;};//var event = new Event('build');//// Listen for the event.//elem.addEventListener('myEvent1', function (e) { //	alert('hello');// }, false);//// Dispatch the event.//elem.dispatchEvent(getEvent("myEvent1"));